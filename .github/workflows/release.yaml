name: release
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'image tag prefix'
        default: 'rc'
        required: true

permissions:
  contents: read # for actions/checkout to fetch code

env:
  LIBCRYPTO_VERSION: "3.3.2-r0"
  ECR_REGISTRY: 167290662341.dkr.ecr.us-west-2.amazonaws.com
  TF_CONTROLLER_REPOSITORY: ${{ github.event.repository.name }}
  TF_RUNNER_REPOSITORY: tf-runner
  BRANCH_PLANNER_REPOSITORY: branch-planner
  AWS_REGION: us-west-2

jobs:
  build-push:
    runs-on: ubuntu-latest
    permissions:
      contents: write # needed to write releases
      id-token: write # needed for keyless signing
      packages: write # needed for ghcr access
    steps:
      - name: Check out
        uses: actions/checkout@b80ff79f1755d06ba70441c368a6fe801f5f3a62 # v4.0.0
      - name: Set up yq
        uses: frenck/action-setup-yq@c4b5be8b4a215c536a41d436757d9feb92836d4f # v1.0.2
        with:
          version: 4.14.1
      - name: Setup Kustomize
        uses: fluxcd/pkg/actions/kustomize@1bfad582060d2d6e464756fbd5d7a2b2fa4f75b9 # main
      - name: Setup Cosign
        uses: sigstore/cosign-installer@59acb6260d9c0ba8f4a2f9d9b48431a222b68e20 # v3.5.0
      - name: Setup Syft
        uses: anchore/sbom-action/download-syft@e8d2a6937ecead383dfe75190d104edd1f9c5751 # v0.16.0
      - name: Prepare
        id: prep
        run: |
          VERSION="${{ github.event.inputs.tag }}-${GITHUB_SHA::8}"
          if [[ $GITHUB_REF == refs/tags/* ]]; then
            VERSION=${GITHUB_REF/refs\/tags\//}
          fi
          echo "BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_OUTPUT"
          echo "VERSION=${VERSION}" >> "$GITHUB_OUTPUT"
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v1
      - name: Setup QEMU
        uses: docker/setup-qemu-action@68827325e0b33c7199eb31dd4e31fbe9023e06e3 # v3.0.0
        with:
          platforms: all
      - name: Setup Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@d70bba72b1f3fd22344832f00baa16ece964efeb # v3.3.0
        with:
          buildkitd-flags: "--debug"
      - name: Publish multi-arch tofu-controller container image
        uses: docker/build-push-action@2cdde995de11925a030ce8070c3d77a52ffcf1c0 # v5.3.0
        with:
          push: true
          no-cache: true
          builder: ${{ steps.buildx.outputs.name }}
          context: .
          file: ./Dockerfile
          build-args: |
            LIBCRYPTO_VERSION=${{ env.LIBCRYPTO_VERSION }}
          platforms: linux/amd64,linux/arm64 #,linux/arm/v7
          tags: |
            ${{ env.ECR_REGISTRY }}/${{ env.TF_CONTROLLER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}
            ${{ env.ECR_REGISTRY }}/${{ env.TF_CONTROLLER_REPOSITORY }}:latest
          labels: |
            org.opencontainers.image.title=${{ github.event.repository.name }}
            org.opencontainers.image.description=${{ github.event.repository.description }}
            org.opencontainers.image.url=${{ github.event.repository.html_url }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ steps.prep.outputs.VERSION }}
            org.opencontainers.image.created=${{ steps.prep.outputs.BUILD_DATE }}
      - name: Publish multi-arch tf-runner base image
        uses: docker/build-push-action@2cdde995de11925a030ce8070c3d77a52ffcf1c0 # v5.3.0
        with:
          push: true
          builder: ${{ steps.buildx.outputs.name }}
          context: .
          file: ./runner-base.Dockerfile
          build-args: |
            LIBCRYPTO_VERSION=${{ env.LIBCRYPTO_VERSION }}
          platforms: linux/amd64,linux/arm64 #,linux/arm/v7
          tags: |
            ${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}-base
          labels: |
            org.opencontainers.image.title=${{ github.event.repository.name }}
            org.opencontainers.image.description=${{ github.event.repository.description }}
            org.opencontainers.image.url=${{ github.event.repository.html_url }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ steps.prep.outputs.VERSION }}
            org.opencontainers.image.created=${{ steps.prep.outputs.BUILD_DATE }}
      - name: Publish multi-arch tf-runner container image
        uses: docker/build-push-action@2cdde995de11925a030ce8070c3d77a52ffcf1c0 # v5.3.0
        with:
          push: true
          no-cache: true
          builder: ${{ steps.buildx.outputs.name }}
          context: .
          file: ./runner.Dockerfile
          platforms: linux/amd64,linux/arm64 #,linux/arm/v7
          build-args: |
            BASE_IMAGE=${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}-base
          tags: |
            ${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}
            ${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}:latest
          labels: |
            org.opencontainers.image.title=${{ github.event.repository.name }}
            org.opencontainers.image.description=${{ github.event.repository.description }}
            org.opencontainers.image.url=${{ github.event.repository.html_url }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ steps.prep.outputs.VERSION }}
            org.opencontainers.image.created=${{ steps.prep.outputs.BUILD_DATE }}
      - name: Publish multi-arch tf-runner-azure container image
        uses: docker/build-push-action@2cdde995de11925a030ce8070c3d77a52ffcf1c0 # v5.3.0
        with:
          push: true
          no-cache: true
          builder: ${{ steps.buildx.outputs.name }}
          context: .
          file: ./runner-azure.Dockerfile
          platforms: linux/amd64,linux/arm64 #,linux/arm/v7 - azure-cli does not install correctly on 32 bit arm
          build-args: |
            BASE_IMAGE=${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}-base
          tags: |
            ${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}-azure:${{ steps.prep.outputs.VERSION }}
            ${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}-azure:latest
          labels: |
            org.opencontainers.image.title=${{ github.event.repository.name }}
            org.opencontainers.image.description=${{ github.event.repository.description }}
            org.opencontainers.image.url=${{ github.event.repository.html_url }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ steps.prep.outputs.VERSION }}
            org.opencontainers.image.created=${{ steps.prep.outputs.BUILD_DATE }}
      - name: Publish multi-arch branch-planner container image
        uses: docker/build-push-action@2cdde995de11925a030ce8070c3d77a52ffcf1c0 # v5.3.0
        with:
          push: true
          no-cache: true
          builder: ${{ steps.buildx.outputs.name }}
          context: .
          file: ./planner.Dockerfile
          build-args: |
            LIBCRYPTO_VERSION=${{ env.LIBCRYPTO_VERSION }}
          platforms: linux/amd64,linux/arm64 #,linux/arm/v7 - azure-cli does not install correctly on 32 bit arm
          tags: |
            ${{ env.ECR_REGISTRY }}/${{ env.BRANCH_PLANNER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}
            ${{ env.ECR_REGISTRY }}/${{ env.BRANCH_PLANNER_REPOSITORY }}:latest
          labels: |
            org.opencontainers.image.title=${{ github.event.repository.name }}
            org.opencontainers.image.description=${{ github.event.repository.description }}
            org.opencontainers.image.url=${{ github.event.repository.html_url }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ steps.prep.outputs.VERSION }}
            org.opencontainers.image.created=${{ steps.prep.outputs.BUILD_DATE }}
      - name: Check images
        run: |
          docker buildx imagetools inspect ${{ env.ECR_REGISTRY }}/${{ env.TF_CONTROLLER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}
          docker pull ${{ env.ECR_REGISTRY }}/${{ env.TF_CONTROLLER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}

          docker buildx imagetools inspect ${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}
          docker pull ${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}

          docker buildx imagetools inspect ${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}-azure:${{ steps.prep.outputs.VERSION }}
          docker pull ${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}-azure:${{ steps.prep.outputs.VERSION }}

          docker buildx imagetools inspect ${{ env.ECR_REGISTRY }}/${{ env.BRANCH_PLANNER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}
          docker pull ${{ env.ECR_REGISTRY }}/${{ env.BRANCH_PLANNER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}
      - name: Sign images
        env:
          COSIGN_EXPERIMENTAL: 1
        run: |
          cosign sign --yes ${{ env.ECR_REGISTRY }}/${{ env.TF_CONTROLLER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}
          cosign sign --yes ${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}-base
          cosign sign --yes ${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}
          cosign sign --yes ${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}-azure:${{ steps.prep.outputs.VERSION }}
          cosign sign --yes ${{ env.ECR_REGISTRY }}/${{ env.BRANCH_PLANNER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}
      - name: Generate release manifests
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          mkdir -p config/release
          kustomize build ./config/crd > ./config/release/${{ env.CONTROLLER }}.crds.yaml
          kustomize build ./config/rbac > ./config/release/${{ env.CONTROLLER }}.rbac.yaml
          kustomize build ./config/manager | yq e '.spec.template.spec.containers[0].env[1].value="${{ env.ECR_REGISTRY }}/${{ env.TF_RUNNER_REPOSITORY }}:${{ steps.prep.outputs.VERSION }}"' - > ./config/release/${{ env.TF_CONTROLLER_REPOSITORY }}.deployment.yaml
          kustomize build ./config/package > ./config/release/${{ env.TF_CONTROLLER_REPOSITORY }}.packages.yaml
          echo '[CHANGELOG](https://github.com/limebike/${{ env.TF_CONTROLLER_REPOSITORY }}/blob/main/CHANGELOG.md)' > ./config/release/notes.md
      - name: Setup Go
        uses: actions/setup-go@cdcb36043654635271a94b9a6d1392de5bb323a7 # v5.0.1
        with:
          go-version-file: go.mod
      - name: Create release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: goreleaser/goreleaser-action@286f3b13b1b49da4ac219696163fb8c1c93e1200 # v6.0.0
        with:
          version: latest
          args: release --release-notes=./config/release/notes.md --skip-validate
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Publish Helm chart
        uses: stefanprodan/helm-gh-pages@0ad2bb377311d61ac04ad9eb6f252fb68e207260 # v1.7.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Trigger the release-runners workflow
        run: |
          curl -XPOST -u "${{ secrets.GITHUB_TOKEN }}:" \
            -H "Accept: application/vnd.github.everest-preview+json" \
            "https://api.github.com/repos/${{ github.repository }}/dispatches" \
            -d '{
            "event_type": "release-runners",
            "client_payload": {
              "version": "${{ steps.prep.outputs.VERSION }}",
              "build_date": "${{ steps.prep.outputs.BUILD_DATE }}",
              "sha": "${{ github.sha }}",
              "repo_desc": "${{ github.event.repository.description }}",
              "repo_url": "${{ github.event.repository.html_url }}"
            }
          }'
